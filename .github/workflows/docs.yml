name: Docs Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  build-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
          enable-cache: true

      - name: Install dependencies
        # sphinx/myst-parser live in the default `dev` dependency-group,
        # which `uv sync` includes automatically.
        run: uv sync

      - name: Build Sphinx docs (warnings as errors)
        working-directory: docs
        run: |
          uv run python -m sphinx -W -b html . _build/html

      - name: Upload built docs
        uses: actions/upload-artifact@v4
        with:
          name: docs-html
          path: docs/_build/html/
          retention-days: 14